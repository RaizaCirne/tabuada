algoritmo "raiz-quadrada"
var
   A: Real
inicio
      // Raiz quadrada
      A <- RaizQ (81) // Raiz quadrada de 81
      Escreval (A)
      
      // Juntando expressões: Int + RaizQ
      A <- Int(RaizQ (90))// Parte inteira da Raiz quadrada de 90
      Escreva (A)
fimalgoritmo